const notes= (userInput) =>
{
    let userInput = '';
    for (filedNote){
        while (condition) {
            userInputer == '';
            window.prompt(`User Input is blank, please enter anotehr input.`);
            continue
        }
    if userInput(wordsEntered) <= CharacterData.length(15) {
        console.log(`You have reached 15 characters, this is to inform you that your note requirement is met and can be submitted.`);


    }
    }
}
module.exports = {notes} 

//! WILDCARD ^^